[package]
name = "folder-auto-banner"
version = "0.6.36"
edition = "2021"
description = "Folder Auto Banner — A directory listing with instant context"
license = "MIT"
homepage = "https://github.com/DraconDev/folder-auto-banner"
repository = "https://github.com/DraconDev/folder-auto-banner"
documentation = "https://docs.rs/folder-auto-banner"
keywords = ["cli", "file-manager", "shell", "productivity"]
categories = ["command-line-utilities"]
default-run = "f"
include = [
    "src/**/*.rs",
    "Cargo.toml",
    "Cargo.lock",
    "README.md",
    "CHANGELOG.md",
    "INSTALL.md",
    "LICENSE",
    "COMPETITORS.md",
    "VISION.md",
    "AUDIT.md",
    "AUDIT-DAEMON.md",
    "SMART_TREE_FINDINGS.md",
    "benches/**/*.rs",
    "tests/**/*.rs",
    "fab-shell.bash",
    "fab-shell.zsh",
    "fabd.service",
    "install.sh",
    "public/**/*",
]

[[bin]]
name = "f"
path = "src/main.rs"

[[bin]]
name = "fabd"
path = "src/daemon.rs"

[dependencies]
# CLI parsing
clap = { version = "4", features = ["derive", "env"] }
clap_complete = "4"

# Rich terminal output
comfy-table = "7"
console = "0.16"
unicode-width = "0.2"

# Fast directory walking
ignore = "0.4"

# Git integration
git2 = "0.21"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bincode = "1.3"

# Directories
directories = "6"

# Progress bars
indicatif = "0.18"

# Human-readable sizes
byte-unit = "5"

# Date/time
chrono = { version = "0.4", features = ["serde"] }

# Error handling
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
toml = "1.1"

# Path handling
path-absolutize = "3.1"

# Glob patterns
glob = "0.3"

# Filesystem watching (for daemon)
inotify = "0.11"
libc = "0.2"

[dev-dependencies]
tempfile = "3"
assert_cmd = "2"
criterion = { version = "0.5", features = ["html_reports"] }
proptest = "1.4"

[[bench]]
name = "performance"
harness = false

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
