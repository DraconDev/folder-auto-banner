# Goal

**Status**: paused
**Policy**: goal
**Auto-continue**: on
**File**: `20260816190511-ck43cd.md`
**Pause reason**: automatic zero-stream abort — no provider activity was observed
**Agent suggests**: The stalled goal turn was aborted safely and the work is saved. /goal resume retries it once; /goal cancel discards it.

## Objective

> Run ONE project audit pass and leave the project in a known state. Scope: also lets run of full uadit of this project see if ther are any problems or opportunities underutilized. (1) Run a FRESH audit pass over the codebase — spawn AT LEAST 3 Explore subagents in ONE message, one per subsystem, so the survey runs in parallel instead of serial through your own context — each with a TIGHT brief: named directories, a ~30-40 tool-use budget, and a ~150-line report cap ('if you near the token limit, stop and report what you have') — hunting real problems: bugs, broken flows, regressions, drift between docs and code, dead code, security holes. Not style nits, not speculative refactors. (2) Append every NEW finding to .pi-glla/audit-loop/findings.md (create the file on the first finding; append-only — never delete, rewrite, or reorder existing lines; never re-report a finding already listed), classified: "- [ ] FIX: SEVERITY: short description (file:line)" for bugs and polish — whether to fix these is NOT a decision — and "- [?] DECIDE: short description (what the choice is, what each side costs)" for direction, trade-offs, and scope questions where two reasonable answers exist. (3) Fix every NEW FIX finding from this pass — real fixes, committed with the repo's configured identity on the current branch (no invented identities or branches) — then check the box: "- [x] … — fixed in <commit>". (4) Change NOTHING for DECIDE findings — RAISE them instead: if any "- [?]" findings exist, present each one to the user with ask_user_question BEFORE calling complete_goal (one question per finding, options from the finding's own two sides plus "Defer"; prose numbered list if ask_user_question is unavailable; Esc = Defer), then record every answer in .pi-glla/audit-loop/findings.md — replace the "- [?]" line with "- [x] DECIDED: <what was chosen> (<date>)" (or "- [x] DEFERRED") so it stops re-surfacing — and queue any chosen work with list_add. (5) Honesty law: never fabricate findings to look busy; never check a box without the fix commit existing; never silently turn a DECIDE into a fix

## Verification contract

the audit pass is complete, every new FIX finding has a fix commit and a checked box in .pi-glla/audit-loop/findings.md, and every DECIDE finding has been raised to the user and recorded as DECIDED/DEFERRED (or the report states plainly that none were found).

## Tasks

- [ ] Remove committed junk & stale standalone files `1`
  - [ ] git rm micro (12MB ELF), 1.txt, note.md (verify nothing references them first) `1.1`
  - [ ] git rm fab-shell.bash fab-shell.zsh scripts/test_lazy_flags.sh (obsolete standalones; f install ships generated wrappers) `1.2`
  - [ ] Fix README claims (generated fab-shell text, top-level flag tables, micro mentions) and install.sh stale comment `1.3`
  - [ ] Update .gitignore for /micro to prevent re-commit `1.4`
- [ ] Delete dead src/cmd files & fix phantom subcommands `2`
  - [ ] git rm src/cmd/completion.rs src/cmd/config.rs src/cmd/install_hook.rs src/cmd/open.rs src/cmd/uninstall_hook.rs `2.1`
  - [ ] Confirm cmd/mod.rs module list and cli Commands enum stay consistent (banner/env/install/config/daemon) `2.2`
  - [ ] cargo build to verify nothing referenced the deleted files `2.3`
- [ ] Fix CLI command bugs (env, install, daemon_mgmt, icons) `3`
  - [ ] env.rs: implement --format json; fix fm→f in fab_clean alias `3.1`
  - [ ] banner.rs FAB_ICONS: honor 'nerd' as enable+glyph mode, '1' as emoji `3.2`
  - [ ] daemon_mgmt.rs: proper exit codes on daemon action failure; clear-cache also wipes data_dir/banner_data `3.3`
  - [ ] install.rs: quote source path in rc line `3.4`
  - [ ] cli/mod.rs: align top-level vs banner-subcommand flags help `3.5`
- [ ] Fix git module parsing & collection bugs `4`
  - [ ] Fix -z rename/copy misparse (skip bare orig-path records via entry[2]==space check) `4.1`
  - [ ] Map UU (and DU/UD) to FileStatus::Conflict so banner conflict path works `4.2`
  - [ ] Safe utf8 truncation helpers for commit subject and build output (char-boundary) `4.3`
  - [ ] Fix pathspec comment + bound file_statuses collection to displayed depth (count-only for deep untracked) `4.4`
  - [ ] Fix git_ahead_behind doc comment `4.5`
- [ ] Fix feature-module bugs (build_status, port_usage, fs) `5`
  - [ ] build_status: pass py_files to py_compile; safe truncation `5.1`
  - [ ] port_usage: add -a to lsof selection; correct +D comment; parse all pid= entries `5.2`
  - [ ] fs: continue counting totals past MAX_ITEMS while capping stored items; remove dead format_size `5.3`
- [ ] Fix daemon robustness (socket, perms, cache, retries) `6`
  - [ ] Client: don't unlink sockets on failures; retry daemon auto-start with backoff loop `6.1`
  - [ ] Daemon::new: ping-check before unlinking stale socket; chmod 0600 socket, 0700 data dir `6.2`
  - [ ] Persist disk banner cache after background size refresh `6.3`
  - [ ] Retry failed_watches entries periodically `6.4`
  - [ ] Add -- to du args, pass paths as OsStr; remove dead DirSize variants, Warm guard, expired branch `6.5`
- [ ] Fix client-side cache & state robustness `7`
  - [ ] banner_data_cache: recursive max_descendant_mtime (bounded) so nested edits invalidate `7.1`
  - [ ] Unify content-probe extension list between banner_data_cache and daemon (single source, incl. .db/.sqlite) `7.2`
  - [ ] cache/mod.rs: move from /tmp/f-cache to per-user dir with 0700; atomic temp+rename writes `7.3`
  - [ ] state/mod.rs: atomic writes via temp+rename for all persistence points `7.4`
- [ ] Remove dead state/test-cache/todo/metrics API `8`
  - [ ] Remove clipboard/pins/sessions public API from state/mod.rs (intentionally removed features) `8.1`
  - [ ] Remove TestResults::save/format_duration; fix future-timestamp expiry `8.2`
  - [ ] Remove dead scan_todos/scan_metrics fns (keep types used by project_insights), dedupe pattern lists `8.3`
- [ ] Fix tests & docs (integration tests, CI, man page, docs) `9`
  - [ ] Rewrite 14 no-op integration tests as real help/exit-code tests for current subcommands `9.1`
  - [ ] alias_test: assert on banner output in b_flag_alone test `9.2`
  - [ ] ci.yml: cargo test -- --test-threads=1 `9.3`
  - [ ] f.1: -o/--oneline; ALIASES.md count; README flag table qualification `9.4`
- [ ] Verify: build, test, clippy, fmt + ledger boxes `10`
  - [ ] cargo fmt --check, cargo clippy -D warnings, cargo test --all-features -- --test-threads=1 `10.1`
  - [ ] Check every findings.md box with the fixing commit hash `10.2`
  - [ ] Final re-read of findings.md for stray un-fixed entries before complete_goal `10.3`
