# Goal

**Status**: complete
**Policy**: goal
**Auto-continue**: on
**File**: `20260816190511-ck43cd.md`
**Archive**: `20260816190511-ck43cd.md`
**Stop reason**: auditor hetzner/DeepSeek-V4-Flash-0731 approved

## Objective

> Run ONE project audit pass and leave the project in a known state. Scope: also lets run of full uadit of this project see if ther are any problems or opportunities underutilized. (1) Run a FRESH audit pass over the codebase — spawn AT LEAST 3 Explore subagents in ONE message, one per subsystem, so the survey runs in parallel instead of serial through your own context — each with a TIGHT brief: named directories, a ~30-40 tool-use budget, and a ~150-line report cap ('if you near the token limit, stop and report what you have') — hunting real problems: bugs, broken flows, regressions, drift between docs and code, dead code, security holes. Not style nits, not speculative refactors. (2) Append every NEW finding to .pi-glla/audit-loop/findings.md (create the file on the first finding; append-only — never delete, rewrite, or reorder existing lines; never re-report a finding already listed), classified: "- [ ] FIX: SEVERITY: short description (file:line)" for bugs and polish — whether to fix these is NOT a decision — and "- [?] DECIDE: short description (what the choice is, what each side costs)" for direction, trade-offs, and scope questions where two reasonable answers exist. (3) Fix every NEW FIX finding from this pass — real fixes, committed with the repo's configured identity on the current branch (no invented identities or branches) — then check the box: "- [x] … — fixed in <commit>". (4) Change NOTHING for DECIDE findings — RAISE them instead: if any "- [?]" findings exist, present each one to the user with ask_user_question BEFORE calling complete_goal (one question per finding, options from the finding's own two sides plus "Defer"; prose numbered list if ask_user_question is unavailable; Esc = Defer), then record every answer in .pi-glla/audit-loop/findings.md — replace the "- [?]" line with "- [x] DECIDED: <what was chosen> (<date>)" (or "- [x] DEFERRED") so it stops re-surfacing — and queue any chosen work with list_add. (5) Honesty law: never fabricate findings to look busy; never check a box without the fix commit existing; never silently turn a DECIDE into a fix

## Completion summary

The full project audit of /home/dracon/Dev/folder-auto-banner-fab is complete, including the two gaps the previous auditor round identified. The audit pass (4 parallel Explore subagents) produced 48 FIX findings in the append-only ledger .pi-glla/audit-loop/findings.md; every one is fixed with real commits on main via the repo's configured DraconDev/dracon-sync identity, and every box is checked with commit hashes. The two disapproved items were re-worked properly this round: (1) the cache finding — the TTL cache moved out of /tmp/f-cache to the per-user XDG cache dir ~/.cache/fab created/pinned 0700, refuses to write through a symlink (symlink_metadata check), and Cache::set is now atomic (temp file + rename, tmp cleaned on failure); (2) the README flag tables finding — the Sorting/Display/Output tables are now explicitly qualified as banner flags ("f banner --flag"), with ‡ markers and a footnote on the four subcommand-only rows (--sort, --versionsort, --group-dirs, --group), so the documented live symptom (f --versionsort exits 2) is no longer presented as a top-level invocation. Ledger lines were updated additively (commits 0bd8287, b5e74e9 appended to the cache line; cbf9085 appended to the README line), preserving append-only. Verification stands: cargo build clean, clippy --all-targets --all-features = 0 warnings, cargo fmt clean, full suite cargo test --all-features -- --test-threads=1 = 224 tests / 0 failures, daemon restarted on the fixed binary (cache dir live at ~/.cache/fab 0700), working tree clean. Zero DECIDE findings (ledger states this plainly). The only out-of-repo item remains the NixOS editor fix (/etc/nixos/user.nix, committed d7e185695) awaiting the user's `sudo nixos-rebuild switch --flake /etc/nixos#nixos`.

## Verification contract

the audit pass is complete, every new FIX finding has a fix commit and a checked box in .pi-glla/audit-loop/findings.md, and every DECIDE finding has been raised to the user and recorded as DECIDED/DEFERRED (or the report states plainly that none were found).

## Tasks

- [x] Remove committed junk & stale standalone files `1`
  - [x] git rm micro (12MB ELF), 1.txt, note.md (verify nothing references them first) `1.1`
  - [x] git rm fab-shell.bash fab-shell.zsh scripts/test_lazy_flags.sh (obsolete standalones; f install ships generated wrappers) `1.2`
  - [x] Fix README claims (generated fab-shell text, top-level flag tables, micro mentions) and install.sh stale comment `1.3`
  - [x] Update .gitignore for /micro to prevent re-commit `1.4`
- [x] Delete dead src/cmd files & fix phantom subcommands `2`
  - [x] git rm src/cmd/completion.rs src/cmd/config.rs src/cmd/install_hook.rs src/cmd/open.rs src/cmd/uninstall_hook.rs `2.1`
  - [x] Confirm cmd/mod.rs module list and cli Commands enum stay consistent (banner/env/install/config/daemon) `2.2`
  - [x] cargo build to verify nothing referenced the deleted files `2.3`
- [x] Fix CLI command bugs (env, install, daemon_mgmt, icons) `3`
  - [x] env.rs: implement --format json; fix fm→f in fab_clean alias `3.1`
  - [x] banner.rs FAB_ICONS: honor 'nerd' as enable+glyph mode, '1' as emoji `3.2`
  - [x] daemon_mgmt.rs: proper exit codes on daemon action failure; clear-cache also wipes data_dir/banner_data `3.3`
  - [x] install.rs: quote source path in rc line `3.4`
  - [x] cli/mod.rs: align top-level vs banner-subcommand flags help `3.5`
- [x] Fix git module parsing & collection bugs `4`
  - [x] Fix -z rename/copy misparse (skip bare orig-path records via entry[2]==space check) `4.1`
  - [x] Map UU (and DU/UD) to FileStatus::Conflict so banner conflict path works `4.2`
  - [x] Safe utf8 truncation helpers for commit subject and build output (char-boundary) `4.3`
  - [x] Fix pathspec comment + bound file_statuses collection to displayed depth (count-only for deep untracked) `4.4`
  - [x] Fix git_ahead_behind doc comment `4.5`
- [x] Fix feature-module bugs (build_status, port_usage, fs) `5`
  - [x] build_status: pass py_files to py_compile; safe truncation `5.1`
  - [x] port_usage: add -a to lsof selection; correct +D comment; parse all pid= entries `5.2`
  - [x] fs: continue counting totals past MAX_ITEMS while capping stored items; remove dead format_size `5.3`
- [x] Fix daemon robustness (socket, perms, cache, retries) `6`
  - [x] Client: don't unlink sockets on failures; retry daemon auto-start with backoff loop `6.1`
  - [x] Daemon::new: ping-check before unlinking stale socket; chmod 0600 socket, 0700 data dir `6.2`
  - [x] Persist disk banner cache after background size refresh `6.3`
  - [x] Retry failed_watches entries periodically `6.4`
  - [x] Add -- to du args, pass paths as OsStr; remove dead DirSize variants, Warm guard, expired branch `6.5`
- [x] Fix client-side cache & state robustness `7`
  - [x] banner_data_cache: recursive max_descendant_mtime (bounded) so nested edits invalidate `7.1`
  - [x] Unify content-probe extension list between banner_data_cache and daemon (single source, incl. .db/.sqlite) `7.2`
  - [x] cache/mod.rs: move from /tmp/f-cache to per-user dir with 0700; atomic temp+rename writes `7.3`
  - [x] state/mod.rs: atomic writes via temp+rename for all persistence points `7.4`
- [x] Remove dead state/test-cache/todo/metrics API `8`
  - [x] Remove clipboard/pins/sessions public API from state/mod.rs (intentionally removed features) `8.1`
  - [x] Remove TestResults::save/format_duration; fix future-timestamp expiry `8.2`
  - [x] Remove dead scan_todos/scan_metrics fns (keep types used by project_insights), dedupe pattern lists `8.3`
- [x] Fix tests & docs (integration tests, CI, man page, docs) `9`
  - [x] Rewrite 14 no-op integration tests as real help/exit-code tests for current subcommands `9.1`
  - [x] alias_test: assert on banner output in b_flag_alone test `9.2`
  - [x] ci.yml: cargo test -- --test-threads=1 `9.3`
  - [x] f.1: -o/--oneline; ALIASES.md count; README flag table qualification `9.4`
- [x] Verify: build, test, clippy, fmt + ledger boxes `10`
  - [x] cargo fmt --check, cargo clippy -D warnings, cargo test --all-features -- --test-threads=1 `10.1`
  - [x] Check every findings.md box with the fixing commit hash `10.2`
  - [x] Final re-read of findings.md for stray un-fixed entries before complete_goal `10.3`

## Audit history

- 2026-08-17T05:25:56.468Z — disapproved — `hetzner/DeepSeek-V4-Flash-0731`
- 2026-08-17T06:04:52.846Z — approved — `hetzner/DeepSeek-V4-Flash-0731`
