//! Uninstall hook command
use anyhow::Result;

pub fn run_uninstall_hook() -> Result<()> {
    println!("🧹 Uninstall hook (not yet implemented)");
    println!("💡 Remove the cfm hook block from your shell config manually.");
    Ok(())
}
